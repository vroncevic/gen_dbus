name: gen_dbus docker checker
on:
  push:
    branches: [ main ]
    aths:
      - 'Dockerfile'
  pull_request:
    branches: [ main ]
    aths:
      - 'Dockerfile'
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Build Docker image
      id: docker_checker
      run: |
        echo Building Docker image
        build_transcript=$(docker build . --file Dockerfile --tag gen_dbus:latest)
        [[ "$build_transcript" == *"Successfully"* ]] && echo ::set-output name=status::success || echo ::set-output name=status::failure
    - name: Check on failures
      if: steps.docker_checker.outputs.status == 'failure'
      run: exit 1
